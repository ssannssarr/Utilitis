[project]
name = "utilitis"
version = "0.1.0"
description = "Simple autoamtion scripts."
readme = "README.md"
requires-python = ">=3.14"
authors = [{ name = "Sann" }]
dependencies = [
    "requests>=2.34.2",
    "rich>=15.0.0",
]
